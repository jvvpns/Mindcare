import 'package:health/health.dart';

void main() {
  print('Health methods:');
  // This is just to see if it compiles and what's available
  Health health = Health();
  // We can't actually run this easily without flutter, but we can check compilation
}
